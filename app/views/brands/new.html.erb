<h1>Brands#new</h1>
<%= form_for(@brand, :html => {:multipart => true}) do |f| %>
  <%= f.label "Brand Name: " %>
  <%= f.text_field :name %>

  <%= f.label "Brand Image: " %>
  <%= f.cl_image_upload :image %>

  <%= f.submit "Add Brand" %>

<% end %>
