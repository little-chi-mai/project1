<h1>New recipes</h1>

<% @recipes.each do |recipe| %>
  <p>
    <% if recipe.image.present? %>
      <%= link_to cl_image_tag(recipe.image, :class => 'thumbnail'), recipe%>
    <% end %>
    <br>
    <%= link_to recipe.name, recipe %>
  </p>
<% end %>
