<%= form_for(@brand, :html => {:multipart => true}) do |f| %>
  <%= f.label "Name: " %>
  <%= f.text_field :name %>

  <%= f.label "Upload Image: " %>
  <%= f.cl_image_upload :image %>

  <%= f.submit "Upload" %>

<% end %>
